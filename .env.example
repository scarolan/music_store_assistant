# OpenAI API Key (required - default provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Gemini API Key (required - default for Music Expert)
GOOGLE_API_KEY=your-google-api-key-here

# Anthropic API Key (optional - for claude-* models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# DeepSeek API Key (optional - for deepseek-* models)
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# ===========================================
# Model Configuration
# ===========================================
# Provider is auto-detected from model name prefix:
#   gpt-*      → OpenAI
#   gemini-*   → Google Gemini
#   claude-*   → Anthropic
#   deepseek-* → DeepSeek

# Supervisor: Routes user intent (simple classification task)
# Default: gpt-4o-mini
SUPERVISOR_MODEL=gpt-4o-mini

# Music Expert: Handles catalog queries
# Default: gpt-4o-mini
MUSIC_EXPERT_MODEL=gpt-4o-mini
#
# Alternative models to try for Music Expert:
# ───────────────────────────────────────────
# OpenAI:
#   gpt-4o-mini      - Fast, cheap, good for demos ($0.15/1M input)
#   gpt-4o           - Smarter, 16x more expensive ($2.50/1M input)
#
# Anthropic:
#   claude-3-5-haiku-20241022  - Fast & cheap, great tool use ($0.25/1M input)
#   claude-3-5-sonnet-20241022 - Balanced quality/speed ($3/1M input)
#   claude-sonnet-4-20250514   - Latest Sonnet, excellent reasoning
#
# Google:
#   gemini-2.0-flash - Very fast, good for creative responses (free tier!)
#   gemini-1.5-pro   - More capable, longer context
#
# DeepSeek:
#   deepseek-chat    - Budget option, surprisingly capable ($0.14/1M input)

# Support Rep: Handles account operations and refunds
SUPPORT_REP_MODEL=gpt-4o-mini

# ===========================================
# OpenTelemetry Configuration (optional)
# ===========================================
# Exports traces to Grafana Cloud or any OTLP-compatible backend
# If not set, OTEL tracing is skipped gracefully

# OTLP endpoint for your Grafana Cloud instance
# Format: https://otlp-gateway-{region}.grafana.net/otlp
# Example regions: prod-us-central-0, prod-eu-west-0
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-us-central-0.grafana.net/otlp

# Authorization header with base64-encoded credentials
# Format: Authorization=Basic {base64(instance_id:api_token)}
# To get credentials:
#   1. Go to Grafana Cloud → Connections → Add new connection → OpenTelemetry
#   2. Copy the "Instance ID" and generate an API token
#   3. Base64 encode: echo -n "instance_id:api_token" | base64
#   4. Use format: Authorization=Basic {result}
# Note: URL-encode if the value contains special characters
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic%20your-base64-encoded-credentials-here

# Service name that appears in Grafana traces (optional)
# Default: music-store-assistant
OTEL_SERVICE_NAME=music-store-assistant
